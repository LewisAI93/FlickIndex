 installations
 -------------
 
 - git clone https://github.com/LewisAI93/FlickIndex
 - cd FlickIndex

   installation:
   - create a .venv in terminal
   - pip install dot-env
   - pip install requests
   - pip install textual
